import CanvasDraw from "react-canvas-draw"

// https://embiem.github.io/react-canvas-draw/
function Draw() {
  return(
    <CanvasDraw canvasWidth={500} canvasHeight={500}/>
  )
}

export default Draw